<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="springcloud.payment.mapper.PaymentMapper">
    <insert id="insert" parameterType="Order" useGeneratedKeys="true" keyProperty="id">
        insert into order_table(serial) value (#{serial})
    </insert>

    <resultMap id="BaseResultMap" type="springcloud.customer.mapper.OrderMapper">
        <id column="id" property="id" jdbcType="BIGINT" javaType="Long"/>
        <result column="serial" property="serial" jdbcType="VARCHAR" javaType="String"/>
    </resultMap>
    <select id="selectOne" parameterType="Long" resultMap="BaseResultMap">
        select *
        from order_table
        where id = #{id}
    </select>

    <resultMap id="ListMap" type="springcloud.customer.mapper.OrderMapper">
        <id column="id" property="id" jdbcType="BIGINT" javaType="Long"/>
        <result column="serial" property="serial" jdbcType="VARCHAR" javaType="String"/>
    </resultMap>
    <select id="selectAll" resultMap="ListMap">
        select *
        from order_table
    </select>
</mapper>